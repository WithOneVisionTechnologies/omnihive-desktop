import React from "react";

const NoModules: React.FC = (): React.ReactElement => {
    return <div style={{ paddingTop: 10, paddingLeft: 20, color: "white" }}>No Modules Placeholder</div>;
};

export default NoModules;
