{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "pwa-chrome",
            "request": "attach",
            "name": "OmniHive Dev",
            "port": 8315,
            "webRoot": "${workspaceFolder}",
            "preLaunchTask": "yarn start"
        }
    ]
}